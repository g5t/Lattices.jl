# this all relies upon GLVisualize to do the heavy lifting (interacting with OpenGL)

# I want to reserve plot for displaying data, and need a suitable function
# name to display a crystal unit cell.
# `display` and `show` are both defined in Base.
# For lack of a better alternative, I'll use `exhibit` for now.

function exhibit(a::Crystal)

end
